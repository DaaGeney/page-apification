export type ThemeType = typeof theme; // This is the type definition for my theme object.

const theme = {
  colors: {
    primary: "#0998B7", //Base color
    secondary: "#00B1BB",
    variant: "#13C9B1",
  },
  text: "#000",
  background: "#fff",
};

export default theme;
